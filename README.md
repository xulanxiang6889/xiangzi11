# Lumen Hub

原创个人 AI 资源与知识服务中心的本地演示 MVP，GitHub 仓库为 `xiangzi11`。内容、订单意向和认证均为 mock，不接入真实支付、账户或外部写入。

## 本地运行

```powershell
pnpm install --ignore-scripts --no-optional
pnpm dev
```

生产构建需要 Next.js 对应平台的 SWC optional binary。
