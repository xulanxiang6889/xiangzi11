"use client"; import {useState} from "react"; export default function Requests(){const [sent,setSent]=useState(false); return <div className="wrap page narrow-page"><p className="eyebrow">REQUESTS</p><h1>说说你要完成什么。</h1>{sent?<div className="notice success">请求已记录在本地演示状态中。我们没有发送邮件或写入外部系统。</div>:<form onSubmit={e=>{e.preventDefault();setSent(true)}}><label>称呼<input required name="name" /></label><label>你希望解决的问题<textarea required name="need" rows={5}/></label><button className="button" type="submit">提交咨询意向</button></form>}</div>}
