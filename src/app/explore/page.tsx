"use client";
import {useMemo,useState} from "react";
const items=[['工作流地图','方法','把模糊需求拆成可执行节点。'],['Prompt 校准包','提示词','为重复任务建立可迭代模板。'],['内容系统诊断','咨询','一次 60 分钟的工作流梳理。'],['创作者资料库','资料库','持续更新的公开方法与案例索引。'],['界面叙事入门','课程','从信息层级到交互反馈的基础课。'],['交付检查清单','模板','上线前的内容与质量检查表。']];
export default function Explore(){const [q,setQ]=useState('');const [cat,setCat]=useState('全部');const shown=useMemo(()=>items.filter(x=>(cat==='全部'||x[1]===cat)&&x.join('').toLowerCase().includes(q.toLowerCase())),[q,cat]);return <div className="wrap page"><p className="eyebrow">EXPLORE</p><h1>找到下一步。</h1><div className="toolbar"><label className="sr-only" htmlFor="search">搜索资源</label><input id="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索资源"/><select value={cat} onChange={e=>setCat(e.target.value)} aria-label="按类别筛选"><option>全部</option><option>方法</option><option>提示词</option><option>咨询</option><option>资料库</option><option>课程</option><option>模板</option></select></div><div className="resource-grid">{shown.map(x=><article className="resource" key={x[0]}><span className="tag">{x[1]}</span><h2>{x[0]}</h2><p>{x[2]}</p><small>本地演示 · 可收藏</small></article>)}</div>{shown.length===0&&<div className="empty">没有匹配内容，换个关键词试试。</div>}</div>}
